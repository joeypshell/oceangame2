# Pass 27 Player-Facing Visual Baseline Decision

Date: 2026-07-09

Issue: #608 `Review Pass 27 visual impact without unrelated baseline drift`

## Decision

No production-slice baseline acceptance is needed for Pass 27.

Pass 27 fixes a transient player-facing rendering problem by keeping the player root stable, clipping the body sprite region, and verifying body/light alignment through repeated reversals. It does not intentionally change terrain, camera framing, player art pixels, boat/base visuals, salvage, hazards, props, map topology, collision, or accepted production-slice baseline views.

## Evidence

Reviewed baseline comparison sheets generated by:

```powershell
python tools/manage_production_slice_baseline.py compare-all
```

Observed result:

- `production_slice_01`: no accepted/current difference.
- `production_slice_02`: no accepted/current difference.
- `production_slice_03`: no accepted/current difference.
- `production_slice_04`: no accepted/current difference.

The focused Pass 27 capture is review-only:

```powershell
& 'C:\Program Files\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' --path . --quit-after 10 --capture-pass-27-player-facing
```

It loads the default production slice, applies deterministic left/right reversals, and frames the final right-facing body and matching light cone in `visual_captures/pass_27_player_facing/production_slice_01_pass_27_player_facing.png`. The generated capture was inspected locally and was not accepted as a baseline.

## Stable Areas

- Production slice terrain and collision presentation.
- Player art source pixels, boat/base/connector, salvage, hazard, and prop art.
- Production camera-test framing.
- Accepted baseline directories.
- Source map topology and generated runtime geometry.
- Overlay layout outside the review-only `Facing review` note.

## Verification

```powershell
python tools/manage_production_slice_baseline.py compare-all
python tools/manage_production_slice_baseline.py check-clean --all-slices
& 'C:\Program Files\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' --headless --path . --import
& 'C:\Program Files\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' --path . --quit-after 10 --capture-pass-27-player-facing
python tools/check_file_lengths.py
git diff --check
```

`check-clean --all-slices` reported all production-slice accepted baseline directories clean. The focused capture was rerun after Godot import so the review did not rely on fresh-worktree loader noise.
