interface:
  display_name: "Parallel Issue Worker"
  short_description: "Claim one GitHub issue safely in parallel"
  default_prompt: "Use $parallel-issue-worker to claim one safe GitHub issue in a dedicated worktree, finish it, and then recheck the queue without monopolizing the batch."
