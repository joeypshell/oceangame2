interface:
  display_name: "Drift Batch Resolve"
  short_description: "Run one bounded milestone through PRs"
  default_prompt: "Use $drift-batch-resolve to audit current origin/main, select or create one bounded milestone batch, resolve only that frozen issue set through PRs and applicable CI in one clean resolver worktree, and stop at player review or closeout without starting another milestone."
