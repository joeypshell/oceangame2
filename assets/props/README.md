# Props

These are first-pass, named sprite assets for collectible salvage and hazards.

They are generated by:

```powershell
python tools\generate_prop_sprites.py
```

Runtime selection stays data-driven through the JSON entity `kind` values in the production-slice maps. Debug markers remain separate overlay shapes; these sprites are normal gameplay art only.
