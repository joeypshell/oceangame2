# Materials

These 32x32 source-driven collectible sprites are generated by:

```powershell
python tools\generate_material_sprites.py
```

Runtime selection uses `material_id`; authored candidate placement and fallback
`kind` values remain unchanged. The fallback prop receives the former material
tint only when a named material texture cannot load.
